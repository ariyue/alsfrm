package com.als.frm.module.frm.biz;

import java.util.Map;

import com.als.frm.comm.dto.AppData;
import com.als.frm.comm.dto.Pagination;

public interface FrmBasConstantBIZ {

	/**
	 * 获取所有常量列表
	 * @return
	 */
	Pagination<Map<String, Object>>  getConstantList(AppData appData);
	
	/**
	 * 获取常量Item列表
	 * @return
	 */
	Pagination<Map<String, Object>>  getConstantItemList(AppData appData);
	
	
	/**
	 * 保存常量
	 * @return
	 */
	Pagination<Map<String, Object>>  saveConstant(AppData appData);
	
	/**
	 * 保存常量
	 * @return
	 */
	Pagination<Map<String, Object>>  removeConstant(AppData appData);
}
